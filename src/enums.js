// Entity contexts
exports.CONTEXTS = {
  GLOBAL: "global"
};

// Entity types.
exports.TYPES = {
  SPEC: "spec",
  TEST: "test",
  PLUGIN: "plugin"
};

// Plugin types
exports.PLUGIN_TYPES = {
  LIB: "lib",
  INLINE: "inline"
};

// Scaffolding commands.
exports.COMMANDS = {
  MAKE_PLUGIN: "makePlugin",
  MAKE_TEST: "makeTest"
};