apiVersion: v1
kind: Service
metadata:
  name: sidecar-injector-webhook-svc
  labels:
    app: sidecar-injector
spec:
  ports:
  - port: 443
    targetPort: 443
    name: https
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: sidecar-injector
