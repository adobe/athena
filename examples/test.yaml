name: ccecoTest
description:
specRef: ccecoSpec #can miss
hooks:
  shouldSkip:
  beforeMe:
  afterMe:
config:
  timeout: 10 #think of other properties
  repeat: 10 #repeat 10 times
scenario:
  given:
  when:
  then: