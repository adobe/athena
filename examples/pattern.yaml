type: perfPattern
name: "prod"
version: "1.0"
description: ""
engine: autocannon
mix:
  - ref: "apiKey" # todo: rename to "perfRun"
    version: "1.0"
    weight: 20%
    config:
      host:
      path:
      port:
      headers:
      body:
      pipelining:
    hooks:
      onInit:
      onDestroy:
      onRequest: > # you will receive this.pool from the metadata of scenario
        headers["Authorization"] = generateRandomToken(`${this.pool:30}`)
      onResponse:
