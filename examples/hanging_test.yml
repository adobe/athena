type: test
name: hanging test
version: "1.0"
description: this test does not belong to any spec
engine: chakram
dependency: # todo: rename to dependsOn
  - name: mc
    type: api
hooks:
  skip:
  beforeEach:
  afterEach:
config:
  timeout: 10 # todo: think of other properties
  repeat: 10  # repeat 10 times
scenario:
  given: >
    this.host = "https://google.com";
  when: >
    this.response = chakram.get(this.host);
  then: >
    expect(this.response).to.have.status(200);
    expect(this.response).to.have.status(200);
