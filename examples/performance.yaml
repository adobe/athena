name: perf
description: someDescr
version: "1.0"
engine: autocannon
type: test
dependecy:
  - name: cceco
    type: api # ??
    version: ">1.0"
hooks:
  shouldSkip:
  onInit:
  onDestroy:
  onRequest:
  onResponse:
config:
  timeout: 60
  host:
  path:
  headers:
  debug: true | false
  threads: 100
  connections: 1000
  keepalive: true
  rps: 3000
scenario:
  pattern:
    ref: "prod"
    version: "1.0"
    config:
      #needed config for pattern
    rampUp:
      every: 10s #or fixed
      rps: 10
      connections: 10
      threads:
      fixed: 30s #or every
    coolDown:
      every: 10s #or fixed
      rps: 10
      threads:
      connections: 10
      fixed: 30s #or every
    spike:
      every: 10s #or fixed
      rps: 10
      threads:
      connections: 10
      fixed: 30s #or every if fixed you need to specify after
      after: 30s
