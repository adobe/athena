type: suite
name: ccecoSuite # required
engine: chakram # required for top level suite, nested values are ignored.
description: some string
dependecy:
  - name: cceco
    type: api
hooks:
  beforeEach: >
    this.token = getServiceToken()
    this.host = "http://localhost:9191/get"
  before:
  after:
  afterEach:
  skip: # function to run before every test and check if it should run or not returns true or false
config:
  timeout: 10
  #think of other stuff
scenario: #will run given inside the test
  given: >
    this.host = "override.host"